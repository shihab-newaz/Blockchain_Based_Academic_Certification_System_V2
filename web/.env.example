# Public — safe to expose in the browser bundle. No secrets go here; the
# issuer JWT never reaches client-side JS (see lib/nest-proxy.ts).
NEXT_PUBLIC_API_URL=http://localhost:3001

# Server-only — used by app/api/* route handlers to reach the Nest API.
NEST_API_URL=http://localhost:3001
